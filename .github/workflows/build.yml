name: Build Project
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Compile main.c and lib.c
        run: gcc main.c lib.c -o binaryoutput
